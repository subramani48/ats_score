import type { RequestHandler } from 'express';
export declare const uploadAndEnqueue: RequestHandler;
export declare const getJobStatus: RequestHandler;
export declare const streamJobProgress: RequestHandler;
